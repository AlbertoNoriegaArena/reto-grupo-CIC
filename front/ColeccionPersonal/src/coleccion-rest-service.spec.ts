import { ColeccionRestService } from './coleccion-rest-service';

describe('ColeccionRestService', () => {
  it('should create an instance', () => {
    expect(new ColeccionRestService()).toBeTruthy();
  });
});
